<?xml version="1.0" encoding="UTF-8"?>
<!--
 ============================================================================
 TICKET-ADV136 — dlq_messages
 WHAT:    One row per message that landed on trade-events-dlq, persisted by
          DlqConsumer. Replayed rows are deleted by DlqAdminController.
 WHY:     Without a durable record, an operator has no way to see what
          failed or to replay a single poisoned event by id.
 ============================================================================
-->
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <changeSet id="009-create-dlq-messages" author="lavinia31">
        <createTable tableName="dlq_messages">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true"/>
            </column>
            <column name="event_id" type="VARCHAR(36)"><constraints nullable="false" unique="true"/></column>
            <column name="trade_ref" type="VARCHAR(30)"><constraints nullable="false"/></column>
            <column name="original_topic" type="VARCHAR(100)"><constraints nullable="false"/></column>
            <column name="kafka_partition" type="INT"><constraints nullable="false"/></column>
            <column name="kafka_offset" type="BIGINT"><constraints nullable="false"/></column>
            <column name="payload" type="VARCHAR(10000)"><constraints nullable="false"/></column>
            <column name="reason" type="VARCHAR(2000)"/>
            <column name="first_seen" type="TIMESTAMP"><constraints nullable="false"/></column>
        </createTable>
        <createIndex tableName="dlq_messages" indexName="idx_dlq_messages_trade_ref">
            <column name="trade_ref"/>
        </createIndex>
        <rollback>
            <dropTable tableName="dlq_messages"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
